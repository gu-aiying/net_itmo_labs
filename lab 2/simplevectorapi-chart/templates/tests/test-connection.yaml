apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "simplevectorapi-chart.fullname" . }}-test-connection"
  labels:
    {{- include "simplevectorapi-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "simplevectorapi-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
